# Training module

Planned role: PiPER-specific OpenPI transforms, norm-stat computation, tiny
overfit, pilot and formal fine-tuning. Every run must record commit, config,
dataset manifest, seed, hardware and output checkpoint hashes.

Training is blocked until the phase gates allow it.
