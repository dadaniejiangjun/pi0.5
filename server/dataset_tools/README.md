# Dataset tools module

Planned role: verify uploaded raw episodes, create immutable canonical raw
views, convert to LeRobot, compute split/statistics manifests and audit raw to
model-input mapping.

Raw data is never overwritten or discarded by conversion.
