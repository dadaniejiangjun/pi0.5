# Policy server module

Planned role: run the official OpenPI WebSocket policy server with a frozen
checkpoint/config/norm-stat bundle, project metadata validation and a health
check. It never accesses CAN or robot low-level control.

Implementation starts after P0/P1. This directory contains no live service in
the plan-only turn.
