# Robot client

Planned role: capture observations, apply the audited model preprocessing,
attach request/response telemetry and communicate with the remote policy
server. It does not directly bypass `PiperSafetyBridge`.
